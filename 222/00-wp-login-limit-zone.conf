# Global soft zone for server 222 — legacy, kept for compatibility
# Real protection is in 01-wp-limit-zones.conf (wp_login_222)
# Updated: v2026-03-28
limit_req_zone $binary_remote_addr zone=wp_login_soft_global:20m rate=6r/m;
