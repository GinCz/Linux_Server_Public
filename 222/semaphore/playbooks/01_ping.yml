---
# Playbook 1: Ping all servers
# Version: v2026-03-28
# = Rooted by VladiMIR | AI =

- name: Ping all servers
  hosts: all_servers
  gather_facts: false

  tasks:
    - name: Ping
      ansible.builtin.ping:

    - name: Show result
      ansible.builtin.debug:
        msg: "{{ inventory_hostname }} ({{ ansible_host }}) — OK"
